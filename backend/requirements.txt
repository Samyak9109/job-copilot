# Core runtime
fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=21.2
pymongo>=4.7
pydantic>=2.6
pydantic-settings>=2.1
email-validator>=2.1
python-multipart>=0.0.9
PyJWT>=2.8
bcrypt>=4.1
pdfplumber>=0.10
python-docx>=1.1

# LLM orchestration (LangChain LCEL). Only langchain-core + the provider
# integrations are imported; the umbrella `langchain` meta-package is not needed.
langchain-core>=0.1.40
langchain-google-genai>=1.0.0
langchain-openai>=0.1.0

# Search grounding for interview prep
tavily-python>=0.3.0

# NOTE: The real Cognee SDK is an OPTIONAL heavy dependency, pinned separately in
# requirements-cognee.txt. It is only needed when COGNEE_MODE=cognee; otherwise the
# app uses its built-in local semantic store. Install it with:
#   pip install -r requirements-cognee.txt   (requires Python <= 3.12)
