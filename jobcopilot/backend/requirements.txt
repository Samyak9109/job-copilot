fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.1
email-validator>=2.1
python-multipart>=0.0.9
PyJWT>=2.8
bcrypt>=4.1
pdfplumber>=0.10
python-docx>=1.1

# LLM orchestration (LangChain LCEL). Only langchain-core + the provider
# integrations are imported; the umbrella `langchain` meta-package is not needed.
langchain-core>=0.1.40
langchain-google-genai>=1.0.0
langchain-openai>=0.1.0

# Long-term memory (Cognee). Heavy; optional at runtime.
# The app degrades to a local semantic store if this is absent (see cognee_service.py).
cognee>=0.1.15

# Search grounding for interview prep
tavily-python>=0.3.0
